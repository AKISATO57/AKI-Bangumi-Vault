# Bangumi 保管库 v0.29.4

## 调整

- 公共标签改为纯 Bangumi API 获取，不再抓取官网网页。
- 公共标签区域仍命名为「公共标签」。
- 将「Bangumi记录更新」改名为「API更新时间」，避免误解为网页上显示的收藏时间。
- 说明 API 标签可能与网页展示略有差异。

## 说明

Bangumi API 的 `updated_at` 字段来自用户收藏接口，但官方文档说明它不代表条目收藏时间，也不应依赖其行为。工具会继续记录「首次备份」和「本地同步」作为本地侧可靠时间。
